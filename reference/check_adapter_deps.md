# Check Package Dependencies

Check Package Dependencies

## Usage

``` r
check_adapter_deps(adapter)
```

## Arguments

- adapter:

  An estimator_adapter object

## Value

TRUE if dependencies available, throws error otherwise
