# Normalize and validate a noise specification

Normalize and validate a noise specification

## Usage

``` r
normalize_noise_spec(noise_spec = NULL)
```

## Arguments

- noise_spec:

  List with noise configuration. NULL uses defaults.

## Value

Validated noise_spec list with all fields populated.
