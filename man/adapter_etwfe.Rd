% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter_etwfe.R
\name{adapter_etwfe}
\alias{adapter_etwfe}
\title{Extended TWFE (Wooldridge) Adapter}
\usage{
adapter_etwfe()
}
\value{
An estimator_adapter for ETWFE estimation
}
\description{
Translates between staggeredpower's common interface and the \code{etwfe} package.
Supports both linear and Poisson (for rare events/counts) estimation.
}
\details{
The etwfe package implements Wooldridge's Extended TWFE approach, which:
\itemize{
\item Linear mode (family = NULL): Equivalent to CS/did2s, additive PT assumption
\item Poisson mode (family = "poisson"): Multiplicative PT assumption on log-rate
}

For Poisson mode with count data:
\itemize{
\item Uses count as outcome with log(population) as offset
\item Coefficients are on log-rate scale
\item ATT is back-transformed to rate scale for comparability
}
}
\examples{
\dontrun{
adapter <- adapter_etwfe()
result <- adapter$fit(data = my_data, outcome_var = "y", family = "poisson", ...)
std_result <- adapter$extract(result)
}
}
