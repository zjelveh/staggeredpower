% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_noise.R
\name{calibrate_noise}
\alias{calibrate_noise}
\title{Calibrate the latent-noise spec (sigma, rho) from data}
\usage{
calibrate_noise(
  data,
  outcome,
  unit_var,
  time_var,
  treat_ind_var,
  family = c("rate", "share"),
  pop_var = NULL,
  total_count_var = NULL,
  rate_scale = 1e+05,
  n_cal_sims = 60L,
  seed = 1L,
  min_periods = 5L
)
}
\arguments{
\item{data}{Panel data (data.table or data.frame).}

\item{outcome, unit_var, time_var, treat_ind_var}{Column names.}

\item{family}{"rate" (Poisson-lognormal) or "share" (logit-normal-Binomial).}

\item{pop_var}{Population column (required for family="rate").}

\item{total_count_var}{Binomial denominator column (required for family="share").}

\item{rate_scale}{Rate denominator scale (default 1e5).}

\item{n_cal_sims}{Sims per grid point in the sigma solve (default 60).}

\item{seed}{RNG seed for the calibration sim (default 1).}

\item{min_periods}{Minimum untreated periods per unit to enter the SD target (default 5).}
}
\value{
A list with \code{latent_sigma}, \code{latent_rho}, \code{sigma_analytic},
\code{sigma_range}, \code{target_per_unit_sd}, and \code{family}.
}
\description{
Derives the multiplicative latent-shock log-SD (\code{latent_sigma}) and its
AR(1) serial correlation (\code{latent_rho}) from the observed panel, so the
power DGP reproduces the outcome's real dispersion instead of hand-set values.
}
\details{
\strong{Target.} \code{latent_sigma} is solved so the DGP's \emph{simulated}
per-unit residual SD -- measured off the same TWFE fit the estimators use, so
it accounts for re-fitting on noisy data -- matches the \emph{real} per-unit
residual SD. This is a like-for-like match to the cluster-level dispersion the
estimators' cluster-robust SEs depend on. \code{latent_rho} is the pooled
within-unit AR(1) coefficient of the residuals. A cheaper closed-form
\code{sigma_analytic} (moment match, no re-fit) is returned as the low end of a
sensitivity band -- report \code{sigma_range}, do not treat one value as exact.
}
